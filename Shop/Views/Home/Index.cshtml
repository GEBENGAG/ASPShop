@{
    ViewData["Title"] = "Home Page";
}

<div class="mt-3">
        <table class=" table" id="tableCountries" >
        <thead>Goods</thead>
        @foreach(var item in Model)
        {
            <tr> 
                <td >@item.Name</td>
                <td >@item.Price</td>
                <td ><a asp-controller="Home"  asp-action="Sort" asp-route-name="@item.Category.Name">@item.Category.Name</a></td>
            </tr>
        }
    </table>
</div>